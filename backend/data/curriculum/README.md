# Curriculum JSON

`day-01.json` through `day-30.json` define learning content and interaction modes. The React activity engine in `src/main.jsx` renders the interaction. Learning is deliberately separate from assessment and practical work.
